import * as React from 'react';

export default function PerfilView({ shopId, user }: { shopId: string; user: any }) {
  return (
    <div className="p-6">
      <h2 className="text-xl font-bold mb-4">Perfil</h2>
      <p className="text-gray-500">Modulo em desenvolvimento.</p>
    </div>
  );
}
